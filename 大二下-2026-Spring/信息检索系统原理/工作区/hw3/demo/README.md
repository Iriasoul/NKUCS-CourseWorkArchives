# HW3: 小型学术文献语义检索系统 (Demo)

## 项目简介
本项目是一个轻量级的信息检索系统 Demo，旨在解决研究人员在管理大量本地文献时，难以通过自然语言描述快速定位目标文献的痛点。
系统核心采用 `TF-IDF` 文本特征提取与 `Cosine Similarity` (余弦相似度) 算法，实现了高效的语义相关度计算与文献召回。

## 环境配置
1. 确保已安装 Python 3.8 或以上版本。
2. 安装依赖：
   
```bash
   pip install -r requirements.txt